# ─── SMTP Configuration ───────────────────────────────────────────────────────
# Copy this file to .env.local for local development.
# In Vercel: Settings → Environment Variables → add each key below.

# Your SMTP server hostname
SMTP_HOST=smtp.example.com

# SMTP port — typically 587 (STARTTLS) or 465 (SSL)
SMTP_PORT=587

# Set to "true" if using port 465 (SSL), otherwise "false"
SMTP_SECURE=false

# SMTP login credentials
SMTP_USER=your-email@example.com
SMTP_PASS=your-smtp-password

# The "From" address shown in the received email
SMTP_FROM=sessions@plusquam.studio

# Where submission emails should be delivered
SMTP_TO=team@plusquam.studio
